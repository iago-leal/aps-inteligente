---
commit: a455f390ec8729657e48ac0043cb693f5c8cd783
feature: default_feature
start_time: '2026-07-27T02:27:16.264398+00:00'
status: inactive
---

## O que foi feito
- **A feature 018 ganhou plano técnico: `/reversa-plan` executado, seis artefatos gerados.** `roadmap.md` (143 linhas, treze decisões D-01 a D-13), `investigation.md`, `data-delta.md`, `onboarding.md` e dois contratos de saída em `interfaces/` — metadados HTML das seis rotas e manifesto PWA. O ponteiro `.reversa/active-requirements.json` passou a `current-stage: plan`, com `requirements` e `clarify` em `stages-completed`.
- **O eixo do plano é um artefato só, e ele serve a quatro requisitos ao mesmo tempo.** `tests/apoio/inventario-textual.json`, gerado por `scripts/inventariar-textos.mts` e versionado, é a lista fechada de RF-02, o oráculo que congela a prosa revisada em RF-06, a entrada do verificador de RF-05 e a linha de base da comparação de RF-07. A extração percorre a **árvore sintática** do TypeScript (compilador já é devDependency), não expressões regulares; a **classe** de cada literal é declarada num mapa exaustivo, e candidato sem entrada faz o gerador parar.
- **Três achados do exame do código mudaram o plano, e nenhum estava no requirements.** Primeiro, **`W022`** do `regression-watch.md` da 017 vigia como modo de falha exatamente o que a 018 passa a fazer por decisão de `MD-0015` — "rótulo 'corrigido' para a norma culta". Segundo, os dois rótulos de §2.4 **não estão** no oráculo congelado `casos-oraculo-puericultura.json`, que só guarda valores da OMS e do INTERGROWTH: nenhum passo depende de reger o congelador nem de ter as fontes de `referencias/` em mãos, o que remove um risco real de execução. Terceiro, a varredura de conferência diverge da heurística do requirements — 411 literais candidatos contra ~276 estimados, 251 asserções de texto em 14 arquivos contra 224 em 33 —, e é por isso que o gerador roda antes de qualquer reescrita.
- **Duas fichas novas, ambas de alcance além da feature.** `MD-0016` consolida o inventário como dado gerado com classe declarada à mão (D-02, D-03, D-04, D-07, D-08), com oito descartes registrados — entre eles o catálogo de mensagens `i18n`, que resolveria o inventário por construção mas apagaria a distinção entre citação e prosa autoral de que a feature depende, e o **Vale**, que **passa** no filtro de longevidade (repositório ativo em 24/07/2026) e por isso precisou ser descartado por adequação, não por abandono. `MD-0017` fixa a doutrina que `W022` obrigou a formular: watch item revogado por decisão posterior é **reescrito no arquivo onde nasceu**, apontando a ficha que o revogou, conservando a parte da vigilância que a decisão não alcançou.
- **Uma correção factual ao requirements, registrada no plano.** O que §2.4 chamou de "oráculos congelados do domínio" são, na verdade, oito asserções literais em três arquivos de teste (`classificacao.test.ts`, `fachada.test.ts`, `puericultura.test.tsx`), mais dois comentários que justificam a concordância destoante e que precisarão ser reescritos junto, sob pena de o código explicar o oposto do que faz.
- **Duas buscas ativas foram feitas antes de descartar ferramenta**, pelo princípio de não confiar na memória: o estado do Vale e as diretrizes de conteúdo do Primer. A segunda produziu registro próprio no `investigation.md` §5 — o Primer prescreve tom informal e nível de leitura de sétima série, calibrado para público geral em inglês, e **não** se estende à prosa desta plataforma; a adoção segue onde a feature 004 a colocou, em tokens e componentes.
- Sessão **estritamente documental**: nenhum código de aplicação, teste ou configuração foi tocado. Não houve build nem suíte, porque não havia o que verificar. Commit `a455f39`.

## Próximos passos
- **`/reversa-to-do` da feature 018.** É o passo do ciclo, e a ordem de execução da seção 8 do roadmap já está desenhada para a decomposição. Duas fases merecem ação própria: a **classificação exaustiva** dos ~411 literais candidatos, que é o único risco de dimensionamento médio do plano, e a passagem por **`/reversa-principles`**, que abre a execução por D-09 — o princípio IX precisa existir antes do guia, que remete a ele.
- **Validar as três premissas 🟡 da 017 com o prescritor** e promover D-09 daquela feature a 🟢: a medição de T049 já a confirmou, e falta o registro formal.
- Manter no radar a **re-extração `/reversa` nº 4**, que absorveria a 017 e superaria os adendos 015, 016 e 017. O custo cresce a cada feature acumulada, e a 018 acrescentará mais uma.

## Pendências / bloqueios
- **`W022` da 017 é a pendência mais urgente da 018, e ela tem prazo.** Enquanto não for reescrito por D-11, a spec afirma o contrário do que o código fará, e a próxima verificação de regressão acusará **vermelho legítimo** sobre decisão deliberada. A reescrita acontece no `/reversa-coding`; até lá, é dívida conhecida e datada.
- **Três premissas do plano, nenhuma bloqueante**, na seção 4 do roadmap: L-08 (se a justificativa clínica é obrigatória, facultativa ou vedada na mensagem de validação — o guia decide durante a redação); L-07 (o `logoComoTitulo` que `_reversa_sdd/domain.md` §7.2 ainda descreve, herdado, pertence à re-extração nº 4); e o **dimensionamento da superfície textual**, único de risco médio — se os 411 candidatos se confirmarem, o mapa de classificação é o gargalo da feature.
- **O custo declarado de `MD-0016`:** o mapa exaustivo cobra uma entrada por literal novo, para sempre. É o preço da falha ruidosa e o efeito é desejado, mas o sinal de que a decisão foi mal tomada é observável — o mapa preenchido em massa sem leitura o converteria em carimbo e devolveria a inferência pela porta dos fundos.
- **Três premissas 🟡 da 017 a validar pelo prescritor**, mantidas por decisão: (1) os três anos da correção de idade valem **1095 dias** (D-16); (2) a **idade cronológica** governa a posição de medida esperada no prematuro; (3) o escore z se exibe com **uma casa decimal** (D-13).
- **Dívida de higiene alheia à feature:** `npm run format:check` segue acusando centenas de arquivos fora de formato, quase todos documentação pré-existente do Reversa. Não é gate do CI; resolve-se com um `--write` de uma vez, em ticket próprio.
- Rastreamento preventivo por perfil — PAUSADA (26/07/2026), aguardando a chave da API USPSTF (AHRQ Prevention TaskForce), solicitada em 23/07/2026 a `uspstfpda@ahrq.gov`, sem resposta. Retomar por `/reversa-requirements` quando a chave chegar.

## Ponteiros
- **Feature ativa: 018**, estágio `plan`. Sete arquivos em `_reversa_forward/018-revisao-linguagem-textos/`: `requirements.md` (291), `roadmap.md` (143), `investigation.md` (143), `data-delta.md` (100), `onboarding.md` (169) e `interfaces/{metadados-html,manifesto-pwa}.md`.
- **A regra de `MD-0016`, em uma linha:** a superfície textual é dado gerado; a classe de cada literal é decisão declarada. O que a máquina extrai é o literal; o que ela nunca infere é a que classe ele pertence.
- **A regra de `MD-0017`, em uma linha:** watch item revogado por decisão posterior é reescrito onde nasceu, apontando a ficha que o revogou — nem apagado, nem contornado por item novo noutro arquivo.
- **Onde a declaração de RF-10 vai morar, e a correção do ponteiro anterior:** não dentro da `NOTA_PROVENIENCIA`, como se supunha, e sim em **constante nova** `NOTA_CORRECAO_DE_CONCORDANCIA` ao lado dela em `models/puericultura/fonte-clinica.ts` (D-06). São dois assuntos distintos, e a `NOTA_PROVENIENCIA` já opera com **dois pares de travessão** contra o teto de um par de RN-03 — ela própria é alvo da revisão de RF-03.
- **A ordem de execução que o `actions.md` tem de preservar** (roadmap §8): princípio IX → guia → aparato com a suíte verde → reescritas autorais → a exceção da citação em ato único → asserções atualizadas → `W022` reconciliado → gates.
- Microdecisões em `.harness/decisoes/`: `MD-0001` a **`MD-0017`**. `MD-0014` segue `superado-parcialmente`. Índice em `.harness/microdecisoes.md`.
- **Adendos vigentes:** 015, 016 e 017; os de 001 a 014 foram superados pela re-extração nº 3 (commit `ab075ac`).
- **Como conferir a feature quando ela existir:** `onboarding.md` traz o roteiro em dez passos, executável de clone limpo, sem as fontes de `referencias/`. O passo 5 é o que nenhum teste substitui — abrir `/puericultura/crescimento` e verificar se o prescritor, lendo **só a tela**, entende por que a caderneta diz "Comprimento adequada" e o produto diz "Comprimento adequado".
- Produção: https://apsinteligente.app · saúde em `/api/v1/status`. SHA `7e0a522`, conferido na sessão anterior; **esta sessão não tocou código, logo produção segue nele**.

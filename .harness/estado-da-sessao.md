---
commit: 91125073513d10c9daa36ec08778b09f88c94d24
feature: default_feature
start_time: '2026-07-27T01:11:13.405137+00:00'
status: inactive
---

## O que foi feito
- **A feature 017 fechou: 39 → 52 das 52 ações.** Correram as duas fases que faltavam — a 4 (Integração, T040 a T048) e a 5 (Polimento, T049 a T052). O quinto domínio deixou de ser motor sem porta: há formulário, painel, proveniência, contêiner, tela, rota `/puericultura/crescimento`, folha de estilo própria, teste de integração e e2e. O cartão da home, que desde a primeira rodada apontava para o vazio por força da regra de anti-drift do README, passou a levar a algum lugar.
- **Três decisões de tela, e todas evitam duplicar regra clínica na apresentação.** (1) *A tela formata, nunca recalcula*: o escore sai com uma casa decimal e sinal explícito inclusive no zero (D-13), e o valor bruto fica intacto na saída. (2) *O título do bloco é neutro* — "Comprimento/estatura para a idade" —, porque escrever ali o substantivo que a fonte troca aos 730 dias seria a segunda implementação daquela fronteira, na camada mais volátil do sistema; o nome certo vem do rótulo literal que o domínio devolve. (3) *A proveniência antecede o número*: RF-13 pede o bloco fora do painel, e ele está visível desde o carregamento, já que os limites da fonte valem para quem ainda vai digitar.
- **D-09 saiu de premissa 🟡 e virou medição.** As sete rotas existentes têm *first load* **bruto idêntico byte a byte**; a rota nova paga sozinha os +80,3 kB gzip das tabelas. O *code-splitting* por rota isolou as 12.964 linhas `L/M/S` sem intervenção alguma, e a porta de D-08 (repositório injetável) segue aberta sem ter sido usada. Registro em `medicao-bundle.md`, como T049 exigia — fora das notas de execução.
- **T050 não precisou da exceção que previa.** Cobertura de `models/**` em **97,02%** de statements (96,05 branches, 98,33 funções, 97,16 linhas) com o quinto domínio dentro, **sem exclusão alguma**: os módulos de dados gerados são integralmente cobertos por serem importados, porque só exportam literais. `vitest.config.ts` ficou intocado — não houve ajuste, silencioso ou não.
- **T052 achou dois excessos reais de teto, e os dois foram corrigidos.** `validarMedidas` tinha 62 linhas por repetir três vezes o mesmo bloco de faixa: virou a tabela `MEDIDAS_VALIDAVEIS`, com 40, e acrescentar uma quarta medida um dia passa a ser acrescentar uma linha. O formulário repetia cinco vezes a anatomia campo → erro → blur: ganhou o subcomponente `CampoNumerico` e caiu de 258 para 226 linhas. Nenhuma mensagem, código de ofensor ou rótulo mudou, e os 218 testes passaram sem edição — a prova de que a refatoração foi de forma.
- **A prova cresceu de 625 para 642 testes em 45 arquivos**, mais **36 e2e**, todos verdes, com `typecheck`, `eslint` e `prettier` limpos. A rota nova nasce com **zero violação axe**, antes e depois do resultado, e `e2e/axe-baseline.json` ficou **intocado**: a linha de base existe para tolerar dívida herdada, e não havia nenhuma a tolerar.
- **Três achados de ferramenta e de ambiente, registrados para quem repetir.** (1) O **`next build` do Next 16 com Turbopack não publica mais o *First Load JS***; a comparação de D-09 foi reconstruída de `.next/build-manifest.json`, que mede o artefato e não o relatório. (2) **e2e vermelho em bloco é suspeita de servidor obsoleto, não de regressão**: a primeira execução falhou em 35 dos 36 testes, inclusive os que a rodada não tocou, porque o Playwright reutilizou um `next-server` de quatro horas antes; encerrado o processo, tudo passou. (3) O `next build` reescreve `next-env.d.ts`, artefato gerado que se restaura com `git checkout --`.
- **Dívida de outra feature corrigida de passagem:** a tabela de rotas do README omitia a calculadora de risco cardiovascular desde a 014. Foi acrescentada junto com a quinta — um índice de rotas que esconde uma delas engana mais do que ajuda. O README ganhou ainda a seção do procedimento de regeneração das tabelas da OMS, com a leitura do `git diff` vazio como verificação.
- **Uma ficha nova: `MD-0012` — o rótulo clínico é do domínio; a tela nomeia o índice pela forma neutra.** É a única decisão desta rodada cuja alternativa era genuinamente defensável e cuja reversão passaria despercebida: o rótulo continuaria correto enquanto o título mentiria, e o médico veria "Estatura para a idade · Comprimento adequada para idade" tendo de escolher em qual linha acreditar. A ficha nomeia o caminho caso a revisão clínica exija o título específico. As demais decisões da rodada são de implementação e ficam declaradas no cabeçalho do módulo que as realiza e nas notas de execução do `actions.md`.

## Próximos passos
- **`/reversa-sync`** para converger a 017 em `_reversa_sdd/addenda/`. Agora o adendo tem as duas margens — na sessão anterior o fluxo foi aberto e encerrado sem escrever, porque com 36 ações abertas ele descreveria um subsistema que nenhum leitor consegue exercitar.
- **Conferir o CI e o SHA em produção** depois do push desta sessão. É a primeira vez desde a feature 016 que código de aplicação entra: `/puericultura/crescimento` passa a existir no ar, e a home ganha a quarta seção.
- **Validar as três premissas 🟡 com o prescritor** (as duas da rodada do motor mais a de precisão de exibição), e então promover D-09 a 🟢 no `sync`, já que a medição a confirmou.
- Manter no radar a **re-extração `/reversa` nº 4**, que absorveria a 017 inteira — decisão sua, em algum momento futuro.

## Pendências / bloqueios
- **O trabalho desta sessão está commitado em três commits** — `7398b21` (a tela, a rota, a folha e o import no shell), `c8e1009` (as 17 provas de integração e as 5 de e2e) e `9112507` (o polimento, os rastros e a ficha `MD-0012`) —, na mesma lógica de separação das rodadas anteriores: o diff do código fica legível sem os testes no meio.
- **Três premissas 🟡 a validar pelo prescritor**, todas mantidas por decisão: (1) os três anos da correção de idade valem **1095 dias**, pela disciplina de D-16; (2) a **idade cronológica** governa a posição de medida esperada no prematuro, e não a corrigida; (3) o escore z se exibe com **uma casa decimal** (D-13) — a caderneta lê faixa, não centésimo. A primeira muda por até um dia; a segunda desloca a posição esperada em até três meses; a terceira não tem risco clínico.
- **Dívida de higiene alheia à feature:** `npm run format:check` segue acusando centenas de arquivos fora de formato, quase todos documentação pré-existente do Reversa. Não é gate do CI e não é regressão; resolve-se com um `--write` de uma vez, em ticket próprio.
- **Os componentes de tela excedem as 50 linhas por função** — 226 no formulário, 108 no painel —, como já ocorre nas outras calculadoras (174 e 113 na 014). O corpo é JSX declarativo e o teto mira lógica, que fica nos `models/`, onde a maior função tem 40 linhas. Fica declarado em vez de silencioso, e é candidato a subcomponentes se voltar a crescer.
- Rastreamento preventivo por perfil — PAUSADA (26/07/2026), aguardando a chave da API USPSTF (AHRQ Prevention TaskForce), solicitada em 23/07/2026 a `uspstfpda@ahrq.gov`, sem resposta. Retomar por `/reversa-requirements` quando a chave chegar.
- **A016 da auditoria não foi corrigida por decisão:** `_reversa_sdd/domain.md` §7.2 regra 11 ainda descreve `logoComoTitulo`, prop removida pelo adendo 016. O plano está certo; o artigo da extração é que está atrasado, e se acerta na próxima re-extração.

## Ponteiros
- Feature ativa: `_reversa_forward/017-puericultura-crescimento/` · estágio `coding` · **52/52 ações**, progresso em `progress.jsonl` (**58 linhas**), rastros em `legacy-impact.md` (reescrito, estado **completo**, com os oito arquivos de tela e rota classificados) e `regression-watch.md` (**W001–W041**; os sete novos vigiam a proveniência fora do painel, a ausência de ritual, o formato do escore, o título neutro, o axe em zero, o isolamento de bundle e a cobertura sem exceção).
- **A tela, em cinco módulos:** `formulario.tsx` (a posição sem default, o campo vazio como ausência) → `resultado.tsx` (um bloco por índice, com padrão, idade usada e página) → `proveniencia.tsx` (fora do painel, lendo o texto congelado do domínio) → `app.tsx` (estado efêmero, motor e data injetáveis) → `tela.tsx` (a `Moldura` com `comInicio`). Rota em `pages/puericultura/crescimento.tsx`, folha em `interface/estilos/puericultura.css`.
- **O domínio, em onze módulos:** `tipos.ts` (o contrato) → `datas.ts` → `idades.ts` (as três idades) → `validacao.ts` → `elegibilidade.ts` (recusa global e parcial) → `medidas.ts` (os 0,7 cm) → `padrao.ts` (a única fronteira entre as réguas) → `oms/{leitura,lms}.ts` → `intergrowth/{equacoes,escore}.ts` → `classificacao.ts` (os rótulos) → `calculadora.ts` (a fachada). Fonte congelada em `fonte-clinica.ts`.
- **As quatro fronteiras, e nenhuma coincide:** a de TABELA aos 1856 dias (`oms/leitura.ts`), a de RÓTULO do IMC aos 1826 (`classificacao.ts`), a dos DOIS ANOS aos 730 — posição de medida, escopo do perímetro cefálico e o substantivo do comprimento — e a das duas RÉGUAS em 27 e 64 semanas pós-menstruais (`padrao.ts`). O cabeçalho de cada uma declara onde estão as outras. **A tela não conhece nenhuma delas**, por decisão (`MD-0012`).
- Microdecisões da 017, série corrente em `.harness/decisoes/`: `MD-0001` a **`MD-0012`**, esta última aberta nesta sessão (o nome do índice e quem o decide).
- **Como medir o bundle sem a tabela do `next build`:** `npm run build` e depois somar, por rota, os chunks de `.next/build-manifest.json` (os comuns de `/_app` mais os próprios). Método e números em `medicao-bundle.md`.
- Cadeia do dado (dev-time): `scripts/baixar-tabelas-oms.mts` → `scripts/lib/planilha.mts` → `scripts/oms/*.mts` → `scripts/gerar-tabelas-oms.mts`. Cadeia gêmea do oráculo, de propósito oposto: `scripts/oraculo/{oms,intergrowth}.mts` → `scripts/congelar-casos-oraculo.mts` → `tests/apoio/casos-oraculo-puericultura.json`. Fontes clínicas fora do git em `referencias/{caderneta,oms,intergrowth}/`.
- **Como reconferir o dado sem entender o gerador:** `node scripts/gerar-tabelas-oms.mts` e depois `git diff`. Vazio significa que a OMS não mudou nada (contrato §6). O mesmo vale para `scripts/congelar-casos-oraculo.mts` e o oráculo. O procedimento está agora no README.
- Adendos vigentes: `_reversa_sdd/addenda/` (001–016). Extração Reversa nº 3: commit `ab075ac`.
- Produção: https://apsinteligente.app · saúde em `/api/v1/status`. **SHA em produção antes desta sessão: `c13dc77`**, e o último código de aplicação publicado era o da feature 016 (`472cb08`). Esta é a primeira sessão desde então que **muda o comportamento em produção**: com o push, `/puericultura/crescimento` entra no ar e a home ganha a quarta seção.

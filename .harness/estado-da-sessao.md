---
commit: 7e0a52292406773804b05cd9bbf9df435a729a00
feature: default_feature
start_time: '2026-07-27T01:58:20.073643+00:00'
status: active
gate_lembrete_fingerprint: 9fe9c16e5a45f4c30ec87b3c7e1f8a1765efaa19
---

## O que foi feito
- **A feature 017 convergiu na extração: `/reversa-sync` escreveu `_reversa_sdd/addenda/017-puericultura-crescimento.md`.** Cenário legado, 52/52 ações, hooks `before-sync`/`after-sync` vazios — nenhum menu de sincronização parcial se aplicou, ao contrário da sessão anterior, em que o fluxo foi aberto e encerrado sem escrever porque 36 ações seguiam abertas. O adendo é a ponte que mantém `architecture.md` e `domain.md` legíveis enquanto eles descrevem quatro domínios e o código já tem cinco; vale até a re-extração nº 4, que o marcará como superado.
- **Vinte impactos registrados na tabela**, distribuídos em 8 `componente-novo`, 7 `regra-alterada`, 2 `delta-de-dados`, 2 `delta-de-contrato-externo` (nenhum real, anotados para leitura) e 1 `regra-nova`. Os três que mais deslocam a leitura da extração: em `architecture.md` §1, onde se lê "quatro domínios independentes", leia-se cinco, e uma camada **dev-time** (`scripts/**`) passa a existir acima das três; em §5, a dívida 🟡 da fronteira de camadas ganha verificação automática **só no quinto domínio**, via `invariantes.test.ts`, e os outros quatro seguem sem ela; em `domain.md` §8, o molde de MD-0009 ganha a segunda espécie de recusa, a **parcial**, que o perímetro cefálico acima de 730 dias inaugurou.
- **Dois desvios deliberados do molde do adendo, e uma ficha para eles: `MD-0013`.** Os quarenta e um watch items são citados **por família** — seis grupos nomeados, com o intervalo `W001`–`W041` e o ponteiro para o `regression-watch.md` — em vez de enumerados um a um como nos dezesseis adendos anteriores, que tinham meia dúzia cada. E o adendo ganhou uma seção final não prevista pelo skill, "Notas de reconciliação", com os três pontos em que a execução decidiu contra o plano: D-09 medida, a exceção de cobertura que não foi necessária e a redação literal da fonte impressa mantida como está. A regra prática que a ficha deixa escrita é de escala: até uns dez itens, enumere; acima disso, agrupe e remeta.
- **Nenhum artefato da extração foi tocado.** O `/reversa-sync` anota, não corrige: `architecture.md`, `domain.md`, as specs de `sdd/` e os demais artefatos-base ficaram intactos, e a única escrita fora de `.harness/` foi o arquivo novo em `addenda/`.
- Sessão **estritamente documental**: nenhum código de aplicação, teste ou configuração mudou. Não houve build nem suíte — os números da 017 seguem os da sessão anterior (642 testes em 45 arquivos, 36 e2e, cobertura de `models/**` em 97,02%).
- **Publicação:** os três commits da feature 017 (`7398b21`, `c8e1009`, `9112507`) **já estavam no remoto** ao abrir esta sessão. Ela acrescentou um só, `7e0a522`, com o adendo, a ficha `MD-0013` e o índice de microdecisões.
- Encerramento não versionado: o estado de sessão ficou como mudança pendente no working tree.

## Próximos passos
- **Conferir o CI e o SHA em produção.** Continua pendente desde a sessão anterior, e agora com um agravante de tempo: o código da 017 já está publicado no remoto, de modo que o deploy da Vercel já correu sem ninguém ter olhado o resultado. `/puericultura/crescimento` deve existir no ar, e a home deve mostrar a quarta seção — verificar em `/api/v1/status` se o SHA é `9112507` ou posterior.
- **Validar as três premissas 🟡 com o prescritor** e, feito isso, promover D-09 a 🟢: a medição de T049 já a confirmou, e falta só o registro formal.
- **Abrir a próxima feature por `/reversa-forward`**, ou manter no radar a **re-extração `/reversa` nº 4**, que absorveria a 017 inteira e superaria os adendos 015, 016 e 017, hoje os três vigentes. A decisão é sua; o custo da re-extração cresce a cada feature que se acumula sobre ela.

## Pendências / bloqueios
- **Três premissas 🟡 a validar pelo prescritor**, todas mantidas por decisão: (1) os três anos da correção de idade valem **1095 dias**, pela disciplina de D-16; (2) a **idade cronológica** governa a posição de medida esperada no prematuro, e não a corrigida; (3) o escore z se exibe com **uma casa decimal** (D-13). A primeira muda por até um dia; a segunda desloca a posição esperada em até três meses; a terceira não tem risco clínico.
- **A016 da auditoria segue não corrigida, por decisão:** `_reversa_sdd/domain.md` §7.2 regra 11 ainda descreve `logoComoTitulo`, prop removida pelo adendo 016. O plano está certo; o artigo da extração é que está atrasado, e se acerta na re-extração nº 4 — o adendo 017 **não** o corrige, porque sincronizar anota, não conserta.
- **Dívida de higiene alheia à feature:** `npm run format:check` segue acusando centenas de arquivos fora de formato, quase todos documentação pré-existente do Reversa. Não é gate do CI e não é regressão; resolve-se com um `--write` de uma vez, em ticket próprio.
- **Os componentes de tela excedem as 50 linhas por função** — 226 no formulário, 108 no painel —, como já ocorre nas outras calculadoras. O corpo é JSX declarativo e o teto mira lógica, que fica nos `models/`, onde a maior função tem 40 linhas. Declarado em vez de silencioso, e candidato a subcomponentes se voltar a crescer.
- Rastreamento preventivo por perfil — PAUSADA (26/07/2026), aguardando a chave da API USPSTF (AHRQ Prevention TaskForce), solicitada em 23/07/2026 a `uspstfpda@ahrq.gov`, sem resposta. Retomar por `/reversa-requirements` quando a chave chegar.

## Ponteiros
- **Adendo desta sessão:** `_reversa_sdd/addenda/017-puericultura-crescimento.md` — vigente desde 28/07/2026, sem linha de superação. Adendos **vigentes**: 015, 016 e 017; os de 001 a 014 foram superados pela re-extração nº 3 (commit `ab075ac`).
- Feature 017: `_reversa_forward/017-puericultura-crescimento/` · estágio `coding`, **52/52 ações** · fontes do adendo em `legacy-impact.md` (estado completo), `regression-watch.md` (**W001–W041**), `requirements.md`, `roadmap.md`, `data-delta.md`, `interfaces/tabelas-de-referencia.md`, `medicao-bundle.md` e `progress.jsonl` (58 linhas: 52 `done`, 5 `verified`, 1 `achado`).
- **As seis famílias dos watch items**, na ordem em que o adendo as agrupa: catálogo e camadas (W001–W004) · cadeia do dado e procedência (W005–W010, W016, W020) · fronteiras e leitura das tabelas (W011–W015) · contrato do domínio, rótulos e escopo (W017, W022–W033) · oráculos congelados (W018, W019, W021, W034) · tela, acessibilidade e custo (W035–W041).
- Microdecisões em `.harness/decisoes/`: `MD-0001` a **`MD-0013`**, esta última aberta nesta sessão (como o adendo de uma feature grande cita quarenta e um watch items sem virar cópia do original). Índice em `.harness/microdecisoes.md`.
- **O quinto domínio, em onze módulos:** `tipos.ts` → `datas.ts` → `idades.ts` → `validacao.ts` → `elegibilidade.ts` → `medidas.ts` → `padrao.ts` → `oms/{leitura,lms}.ts` → `intergrowth/{equacoes,escore}.ts` → `classificacao.ts` → `calculadora.ts` (fachada `CalculadoraCrescimentoInfantil`). Fonte congelada em `fonte-clinica.ts`. A tela em cinco: `formulario` → `resultado` → `proveniencia` → `app` → `tela`.
- **As quatro fronteiras, e nenhuma coincide:** TABELA aos 1856 dias (`oms/leitura.ts`) · RÓTULO do IMC aos 1826 (`classificacao.ts`) · DOIS ANOS aos 730 (posição de medida, escopo do PC e o substantivo do comprimento) · as duas RÉGUAS em 27 e 64 semanas pós-menstruais (`padrao.ts`). A tela não conhece nenhuma delas, por decisão (`MD-0012`).
- **Como reconferir o dado sem entender o gerador:** `node scripts/gerar-tabelas-oms.mts` e depois `git diff`; vazio significa que a OMS não mudou nada (contrato §6). O mesmo para `scripts/congelar-casos-oraculo.mts` e o oráculo. Procedimento no README.
- **Como medir o bundle sem a tabela do `next build`:** `npm run build` e somar, por rota, os chunks de `.next/build-manifest.json`. Método e números em `medicao-bundle.md`.
- Produção: https://apsinteligente.app · saúde em `/api/v1/status`. Último SHA conferido: `c13dc77` (feature 016). O código da 017 já foi publicado no remoto, mas **nenhuma verificação de produção foi feita** nesta sessão nem na anterior.

# Relato de intake — 2026-07-19 21:40

> Contexto: `motor-insulina`. Escrivão: Reversa Debugger. Origem: sessão de validação do
> Reversa Reviewer (pergunta 4 de `_reversa_sdd/questions.md`), respondida pelo usuário no chat.

## Problema 1 — Motor silencioso com HbA1c ausente nos ramos residuais da intensificação

**Relato (validação do Reviewer, pergunta 4):** perguntado se o silêncio do motor é a conduta
desejada quando a HbA1c está ausente nos ramos residuais da intensificação (fora do cenário
EC-10), o usuário médico respondeu:

> "Não, deveria recomendar dosar HbA1c."

**Observações do escrivão:**

- Comportamento observado no código: `models/insulina/regra-intensificacao.ts:99` —
  `if (!temRegular || prePrandiais.length === 0) return;` sem recomendação alguma.
- Ramos alcançáveis do silêncio (a validação EC-10 bloqueia o terceiro):
  1. Esquema **sem Regular** e **sem pré-prandiais** (só jejum), HbA1c ausente.
  2. Esquema **com Regular** e **sem pré-prandiais**, HbA1c ausente.
- Efeito clínico: o prescritor recebe o resultado da titulação basal sem nenhum aviso de que a
  HbA1c é necessária para dirigir a intensificação futura — a informação de que falta o exame
  se perde.
- Não há dose errada emitida; é omissão de orientação.
- Frequência: determinística (todo cálculo que caia nesses ramos).

**Severidade/prioridade registradas:** medium / P2 — proposta do agente (omissão de orientação,
sem dose incorreta; correção pequena e bem delimitada). Ajustável pelo usuário.

**Encerramento do relato:** problema único nesta sessão; usuário pediu o registro diretamente
("Commite a extração e registre o bug via /reversa-debugger").
